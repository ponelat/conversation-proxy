// Proves a custom agent can import its own helpers (D7 runtime import graph).
export { assembledFromParts, systemNote, toAssembledMessage } from "@/assembler/parts";
export const PROMPT = "You are a helpful veterinary assistant.";
